# Captures d'écran — Épreuve Séance 1

Dossier destiné aux captures exigées par l'épreuve (partie 3). À ajouter dès
que Docker fonctionne sur la machine :

1. `docker-compose-ps.png` — la sortie de `docker compose ps` avec les trois
   services (`db`, `backend`, `frontend`) en cours d'exécution.
2. `application.png` — l'application ouverte dans le navigateur, connecté en
   tant qu'administrateur, avec au moins un livre visible dans la liste.

(Dans l'attente, ce fichier sert uniquement à garder le dossier versionné.)
