-- Exécuté automatiquement par le conteneur Docker au PREMIER démarrage
-- (volume vide). Crée la base avec le nom exact attendu par
-- application.properties — les guillemets sont obligatoires à cause des
-- majuscules dans le nom.
CREATE DATABASE "KAFOKAMLybrery48";
